using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Budynek : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("Budynekt Start");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
